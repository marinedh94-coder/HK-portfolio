const dialog=document.querySelector('#image-dialog');if(dialog){const picture=dialog.querySelector('img');document.querySelectorAll('[data-image-title]').forEach(button=>button.addEventListener('click',()=>{document.querySelector('#image-caption').textContent=button.dataset.imageTitle;picture.style.filter=button.classList.contains('crop-two')?'hue-rotate(55deg)':'';dialog.showModal()}));dialog.querySelector('button').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',event=>{if(event.target===dialog){const r=dialog.getBoundingClientRect();if(event.clientX<r.left||event.clientX>r.right||event.clientY<r.top||event.clientY>r.bottom)dialog.close()}})}
